Create repo in github

curl -u 'username' https://api.github.com/user/repos -d '{"name": "nameofrepo"}'

First commit

echo "# Java" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/OlaKa/Java.git
git push -u origin master
